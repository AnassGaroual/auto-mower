package com.mowitnow.automower.domain.model;

/**
 * Represents the commands that can be issued to a mower.
 * D - Turn right
 * G - Turn left
 * A - Move forward
 */
public enum Command {
    D, G, A
}
